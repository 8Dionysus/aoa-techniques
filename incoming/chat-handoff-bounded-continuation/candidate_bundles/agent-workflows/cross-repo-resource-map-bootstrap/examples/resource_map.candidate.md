# minimal example seed

Repos:
- `frontend` -> `src/app/routes.ts`
- `backend` -> `api/openapi.yaml`
- `docs` -> `docs/DEPLOYMENT_FLOW.md`

Why they matter:
- route path
- request contract
- operator rollout notes

The point of the example is that the next operator can see the cross-repo starting surface without reading every repository.
