# minimal example seed

Claim:
- `feature flag file was updated`

Evidence checked:
- latest commit touching `config/feature-flags.json`
- current diff for the same path

Output:
- `verified` or `mismatch`

The point of the example is that handoff trust is anchored to visible repo evidence.
