# minimal example seed

Handoff artifact:
- `summary`
- `done[]`
- `blocked[]`
- `next[]`
- `artifact_refs[]`

Trigger:
- context compaction requested

The point of the example is that the handoff object exists before context loss and carries the minimum continuation packet.
