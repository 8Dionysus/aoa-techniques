# minimal example seed

Open:
- read the handoff note
- check `git status`
- inspect the files named in the handoff

Proceed only if:
- the repo state still matches the expected baseline

The point of the example is that work starts from a visible baseline check rather than memory.
