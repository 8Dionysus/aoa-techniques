# minimal example seed

Trigger:
- commit `abc1234`

Artifact fields:
- `commit_ref`
- `review_scope`
- `findings[]`
- `generated_at`

The point of the example is that the review output is an artifact tied to a visible commit boundary.
