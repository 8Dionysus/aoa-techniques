# duplicate group seed

Example duplicate grouping output:

- `group_id`
- `members[]`
- `representative`
- `confidence`
- `review_required`

The technique groups candidates.
It does not delete files by itself.
