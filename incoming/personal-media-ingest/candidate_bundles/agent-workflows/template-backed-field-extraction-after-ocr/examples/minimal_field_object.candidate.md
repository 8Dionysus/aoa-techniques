# minimal field object seed

Example extracted object:

- `merchant`: maybe
- `date`: maybe
- `amount_total`: maybe
- `currency`: maybe
- `missing_fields[]`
- `source_ref`

The point of the example is not parser completeness.
The point is explicit missing or conflicting fields after OCR.
