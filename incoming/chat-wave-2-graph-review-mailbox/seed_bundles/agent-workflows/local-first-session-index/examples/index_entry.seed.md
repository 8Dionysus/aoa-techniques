# minimal example seed

Index entry:
- `session_id`
- `source_path`
- `started_at`
- `agent_name`
- `keywords[]`

Query result:
- one index hit
- one link back to the original saved artifact

The point of the example is that the index helps navigation without replacing the source artifact.
