# Minimal requirements-design-tasks ladder

Requirement:

- keep review findings searchable after the review run

Design:

- store findings in one bounded artifact with stable references and a small query surface

Tasks:

- define artifact fields
- write one artifact example
- validate stale-finding handling

Each layer constrains the next one without collapsing requirement, design, and tasks into one planning blob.
