# Canonical Readiness

## Technique
- id: AOA-T-0056
- name: channelized-agent-mailbox

## Verdict
- approve for canonical promotion

## Evidence summary

- external origin: the imported technique has a bounded donor contract and explicit exclusions around pub/sub governance, federation, trust and consent layers, analytics, installer flows, and full messaging-platform doctrine
- second context: `aoa-techniques` now records the same named-channel mailbox seam as a documentation-first adaptation with one example and one checklist
- external review: the first import review passed and confirmed the technique is public-safe and bounded at the current scale
- `mycel` provides exact-fit public reinforcement beyond the donor family: an AI CLI agent mailbox with stable `msg_id`, bounded `thread_id` lanes, ordered thread logs, per-relay sync cursors, local outbox retry, read and delivery state, and local ACK rows keyed by logical message ID
- adjacent contrast: `MCP Agent Mail` shows a close agent mailbox and explicit acknowledgment surface, but its license rider keeps it out of the clean primary evidence role for this public canonical proof
- validation strength: the bundle now carries one checklist, one example, a clean external-origin note, a documentation-first second context, and a clean MIT public runtime reinforcement beyond the donor repository family

## Default-use rationale

- this is the right canonical default when the main problem is durable channel transport with replay and explicit acknowledgment across session gaps
- it remains narrower than transcript and history artifacts because it owns live mailbox transport rather than post-capture packaging or indexing
- it also remains narrower than the active handoff narrowing lane because delivery and acknowledgment alone do not authorize continuation, stop, return, or escalation behavior
- it is now the right canonical default when a workflow needs a bounded mailbox or thread lane with replayable history and explicit local acknowledgment state, while keeping remote delivery proof, trust policy, encryption, adapters, and platform behavior outside the reusable technique

## Fresh public-safety check

- review date: 2026-05-12
- result: pass
- sanitization still holds: the bundle keeps only the reusable mailbox seam and excludes donor-specific file formats, MCP commands, trust and federation layers, analytics, and platform packaging
- public reuse check: the example, checklist, and adaptation notes remain understandable without hidden donor-repo context; `mycel` is public MIT evidence, and no source code, private data, credentials, or restricted workflow details are copied into the technique

## Remaining gaps

- no blocker remains for canonical status
- future work can add another non-donor mailbox, queue, or agent-thread implementation, but it must preserve the narrow boundary: named lane, ordered replay or cursor, explicit ack or handled state, and no handoff authorization or messaging-platform doctrine
- keep the `mycel` caveat visible: ACK is local/experimental in the inspected release and should not be sold as remote delivery confirmation

## Recommendation

- move `AOA-T-0056` to `canonical`
- add an adverse-effects review to preserve the boundary between mailbox delivery, local acknowledgment, continuation permission, transcript history, trust policy, and full messaging platforms
