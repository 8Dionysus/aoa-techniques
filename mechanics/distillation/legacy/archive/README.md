# Distillation Archive

This directory preserves closed Distillation evidence packets that no longer
belong in active intake.

Current archive sets:

- [closed-incoming-packets](closed-incoming-packets/README.md): closed donor
  wave packets moved out of root `incoming/` after first-pass queues were
  exhausted and all non-landed tails received final verdicts.

Current behavior lives in [../../parts](../../parts/), not here.
