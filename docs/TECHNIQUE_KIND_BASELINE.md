# Technique Kind Baseline

This is the historical baseline note for the `kind` axis.

The living doctrine now lives in [Technique Kind Guide](TECHNIQUE_KIND_GUIDE.md).
Use this file only when you need the original baseline context or a pointer to
the source files that supported the first kind-axis pass.

Current source pointers:

- [`../config/technique_kind_registry.yaml`](../config/technique_kind_registry.yaml)
- [`../mechanics/distillation/parts/technique-reform-ingress/data/technique_kind_overlay.yaml`](../mechanics/distillation/parts/technique-reform-ingress/data/technique_kind_overlay.yaml)
- [`../mechanics/distillation/parts/technique-reform-ingress/reports/technique_kind_counts.md`](../mechanics/distillation/parts/technique-reform-ingress/reports/technique_kind_counts.md)

Historical note:

- the first kind-axis pass established `domain` as ownership and `kind` as the narrow second selector axis
- `family` stayed optional and scout-only
- `tags` stayed freeform
- the baseline was intentionally kept bounded so the corpus could adopt `kind` without widening into a new policy layer
