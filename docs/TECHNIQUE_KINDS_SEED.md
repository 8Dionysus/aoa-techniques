# Technique Kinds Seed

This is the historical wave1 seed note for the `kind` axis.

The living doctrine now lives in [Technique Kind Guide](TECHNIQUE_KIND_GUIDE.md).
Use this file only when you need the original seed context or a pointer to the wave1 source files.

Current source pointers:

- [`../config/technique_kind_registry.yaml`](../config/technique_kind_registry.yaml)
- [`../data/technique_kind_wave1.yaml`](../data/technique_kind_wave1.yaml)
- [`../reports/wave1_kind_counts.md`](../reports/wave1_kind_counts.md)

Historical note:

- wave1 established `domain` as ownership and `kind` as the narrow second selector axis
- `family` stayed optional and scout-only
- `tags` stayed freeform
- the seed was intentionally kept bounded so the corpus could adopt `kind` without widening into a new policy layer
